# Module Template

Copy this directory to `agent/modules/<new-module>/` to start a new module.

## Quick start

```bash
cp -r agent/modules/_template agent/modules/<your-module>
# rename files, implement the contract
# declare in docs/AGENT.md before deploying
```

See [MODULE_CONTRACT.md](MODULE_CONTRACT.md) for the full contract.
